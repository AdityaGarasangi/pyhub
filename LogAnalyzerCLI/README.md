# LogAnalyzerCLI

A simple Python CLI I built to make digging through big log files less painful. It quickly finds errors, failures, and stopped services, and gives you a clean summary so you don’t have to scroll forever.